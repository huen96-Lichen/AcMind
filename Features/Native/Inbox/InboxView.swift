import SwiftUI

struct InboxView: View {
    var body: some View {
        Text("Inbox View")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(ACColors.pageBackground)
    }
}
